{
  "private": true,
  "name": "bbc-vj-air-quality-assessment-task",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --dest dist",
    "serve": "sirv dist --port 8080 --cors --single --host",
    "start": "preact watch",
    "start:prod": "npm run build && npm run serve",
    "lint": "eslint src --fix"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "dist/"
    ]
  },
  "devDependencies": {
    "babel-preset-preact": "^2.0.0",
    "core-js": "^3.17.3",
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "node-sass": "^6.0.1",
    "preact-cli": "^3.0.0",
    "preact-i18n": "^1.5.0",
    "redux-zero": "^5.1.7",
    "sass-loader": "^10",
    "sirv-cli": "1.0.3"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4"
  }
}
